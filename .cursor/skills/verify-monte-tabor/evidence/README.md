Proof artifacts live here, one subdirectory per feature id (`responsive-layout`, `whatsapp-ctas`, …). Cleanup deletes `../run/` only; it never deletes this directory.

A layout proof is incomplete without `phone-portrait.png`, `tablet-portrait.png`, matching `*.json` probes, and `improvements.md`.
